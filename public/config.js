// Public configuration only. Never add API keys or secrets here.
// Leave apiBase empty when the Node server serves this folder (same origin).
// Set it only if the website and the API are on different hosts.
window.MEMORY_WALLET_CONFIG = { apiBase: "https://ai-memory-wallet-live.yajenn91.workers.dev" };
